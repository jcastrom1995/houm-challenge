import type { ReactNode } from "react";

type Header = {
  children: ReactNode;
};

type Content = {
  children: ReactNode;
};

type Modal = {
  children: ReactNode;
};

export type { Header, Content, Modal };
